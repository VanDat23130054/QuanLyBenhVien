# Database connection settings - update these for your environment
db.url=jdbc:sqlserver://localhost:1433;databaseName=QuanLyBenhVien;encrypt=false
db.user=sa
db.password=YourStrongPassword123!

# Optional: JDBC driver (Maven dependency provided in pom.xml)
db.driverClassName=com.microsoft.sqlserver.jdbc.SQLServerDriver

# Example: override values with environment variables if you prefer
# db.url=${DB_URL}
# db.user=${DB_USER}
# db.password=${DB_PASSWORD}
